/**
 * Camel to Snake Case
 *
 * Convert a camelCase string to snake_case.
 *
 * Examples:
 *   camelToSnake("helloWorld")       => "hello_world"
 *   camelToSnake("getElementById")   => "get_element_by_id"
 *   camelToSnake("alreadylower")     => "alreadylower"
 *   camelToSnake("XMLParser")        => "x_m_l_parser"
 *   camelToSnake("")                 => ""
 *
 * Hint: Loop through each character. When you find an uppercase letter,
 *       add an underscore before it and convert it to lowercase.
 *       You can check if a character is uppercase with:
 *       char === char.toUpperCase() && char !== char.toLowerCase()
 *
 * Pattern: Build and return with character inspection.
 */

function camelToSnake(str) {
  // Your code here
}

module.exports = camelToSnake;
