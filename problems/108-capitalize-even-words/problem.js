/**
 * Capitalize Even Words
 *
 * Given a sentence, return a new string where words at even indices
 * (0, 2, 4, ...) are fully uppercased, and words at odd indices
 * are fully lowercased.
 *
 * Examples:
 *   capitalizeEven("hello world foo bar")
 *     => "HELLO world FOO bar"
 *
 *   capitalizeEven("one two three")
 *     => "ONE two THREE"
 *
 *   capitalizeEven("single")
 *     => "SINGLE"
 *
 *   capitalizeEven("")
 *     => ""
 *
 * Hint: Break apart, transform each piece based on its position,
 *       then put it back together.
 */

function capitalizeEven(str) {

}

module.exports = capitalizeEven;
