/**
 * Unique Sorted
 *
 * Given an array of numbers, return a new array with duplicates removed
 * and the remaining elements sorted in ascending order.
 *
 * Examples:
 *   uniqueSorted([3, 1, 2, 3, 1])    => [1, 2, 3]
 *   uniqueSorted([5, 5, 5])           => [5]
 *   uniqueSorted([4, 2, 7, 2, 1])    => [1, 2, 4, 7]
 *   uniqueSorted([])                  => []
 *
 * Hint: You've already done remove-duplicates (problem 10). This time,
 *       also sort the result. Remember: .sort() on numbers needs a
 *       compare function — (a, b) => a - b — otherwise it sorts as strings.
 *
 * Pattern: Hash map + sort (combining two familiar tools).
 */

function uniqueSorted(arr) {
  // Your code here
}

module.exports = uniqueSorted;
