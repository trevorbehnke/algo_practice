/**
 * Remove Nth From End
 *
 * Given an array and a number n, return a new array with the nth
 * element from the END removed. n is 1-based (n=1 means remove
 * the last element).
 *
 * Examples:
 *   removeNthFromEnd([1, 2, 3, 4, 5], 2)   => [1, 2, 3, 5]
 *   removeNthFromEnd([1, 2, 3], 1)          => [1, 2]
 *   removeNthFromEnd([1, 2, 3], 3)          => [2, 3]
 *   removeNthFromEnd(["a", "b", "c"], 2)    => ["a", "c"]
 *   removeNthFromEnd([1], 1)                => []
 *
 * Hint: If you know the length and the position from the end,
 *       you can figure out the position from the start.
 */

function removeNthFromEnd(arr, n) {

}

module.exports = removeNthFromEnd;
