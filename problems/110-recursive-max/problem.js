/**
 * Recursive Max
 *
 * Given an array of numbers, return the largest number.
 * You must use recursion (no loops, no Math.max with spread).
 *
 * Examples:
 *   recursiveMax([3, 7, 2, 9, 4])    => 9
 *   recursiveMax([1])                 => 1
 *   recursiveMax([-5, -2, -8])        => -2
 *   recursiveMax([10, 10, 10])        => 10
 *
 * Hint: Compare the first element against the max of the rest.
 *       What's your base case when there's only one element?
 */

function recursiveMax(arr) {

}

module.exports = recursiveMax;
