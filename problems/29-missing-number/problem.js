/**
 * Missing Number
 *
 * Given an array containing n numbers from the range 0 to n, find the
 * one number that is missing from the array.
 *
 * Examples:
 *   missingNumber([3, 0, 1])         => 2
 *   missingNumber([0, 1])            => 2
 *   missingNumber([9, 6, 4, 2, 3, 5, 7, 0, 1]) => 8
 *   missingNumber([0])               => 1
 *
 * Hint: If you had ALL numbers from 0 to n, what would their sum be?
 *       The formula for the sum of 0 to n is: n * (n + 1) / 2.
 *       Compare that expected sum to the actual sum of the array.
 *       The difference is your missing number!
 *
 * Pattern: Math trick + sum.
 */

function missingNumber(nums) {
  // Your code here
}

module.exports = missingNumber;
