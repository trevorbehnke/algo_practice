/**
 * Zip Arrays
 *
 * Given two arrays, combine them into an array of pairs. Each pair is an
 * array of [element from arr1, element from arr2] at the same index.
 * If the arrays are different lengths, only zip up to the shorter one.
 *
 * Examples:
 *   zip([1, 2, 3], ["a", "b", "c"])   => [[1, "a"], [2, "b"], [3, "c"]]
 *   zip([1, 2], ["a", "b", "c"])      => [[1, "a"], [2, "b"]]
 *   zip([], [1, 2])                   => []
 *
 * Hint: Math.min(a, b) gives you the smaller of two numbers.
 *
 * Pattern: Build and return with two arrays.
 */

function zip(arr1, arr2) {
  // Your code here
}

module.exports = zip;
