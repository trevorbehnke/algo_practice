/**
 * Sum Digits
 *
 * Given a positive integer, return the sum of its digits.
 *
 * Examples:
 *   sumDigits(123)    => 6       (1 + 2 + 3)
 *   sumDigits(49)     => 13      (4 + 9)
 *   sumDigits(1000)   => 1       (1 + 0 + 0 + 0)
 *   sumDigits(7)      => 7
 *   sumDigits(0)      => 0
 *
 * Hint: Convert the number to a string, then loop through each character.
 *       Convert each character back to a number with Number(char) and
 *       add it to a running total. This is the "build and return" pattern
 *       applied to digits.
 *
 * Pattern: Convert, loop, accumulate.
 */

function sumDigits(num) {
  // Your code here
}

module.exports = sumDigits;
