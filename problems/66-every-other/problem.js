/**
 * Every Other
 *
 * Given an array, return a new array containing every other element,
 * starting from the first.
 *
 * Examples:
 *   everyOther([1, 2, 3, 4, 5])     => [1, 3, 5]
 *   everyOther(["a", "b", "c", "d"]) => ["a", "c"]
 *   everyOther([10])                 => [10]
 *   everyOther([])                   => []
 *
 * Hint: Loop through the array, but increment by 2 instead of 1:
 *       for (let i = 0; i < arr.length; i += 2)
 *
 * Pattern: Step-based iteration.
 */

function everyOther(arr) {
  // Your code here
}

module.exports = everyOther;
